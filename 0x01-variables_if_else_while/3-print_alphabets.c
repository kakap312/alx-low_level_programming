#include <stdio.h>
/**
 * main - entry point
 *
 * Return: termination point
 */
int main(void)
{
	int a;
	int b;

	for (a = 97; a <= 122; a++)
	{
		putchar(a);
	}
	for (b = 60; b <= 90; b++)
	{
		putchar(b);
	}
	putchar('\n');
	return (0);


}
